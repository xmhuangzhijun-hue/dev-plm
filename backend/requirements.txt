# Existing host packages; install/upgrade only after explicit environment approval.
fastapi==0.141.1
uvicorn==0.49.0
PyJWT==2.13.0
python-dotenv==1.2.2
# PostgreSQL driver required in the project virtual environment.
psycopg[binary]>=3.2,<4
# Acceptance tools
pytest==9.1.1
httpx==0.28.1
